# PlatformIO build artifacts
firmware/.pio/
firmware/.vscode/

# OS
.DS_Store

# Claude Code
.claude/

# Node tooling deps (npm install --no-save)
tools/node_modules/
tools/package-lock.json

# Python venv (root and daemon)
.venv/
daemon/.venv/

# Python bytecode cache
__pycache__/
*.pyc

# Logs
merged.log/esp.log
merged.log/merged.log

# Internal agent notes — not part of the project
CLAUDE.md

# Docs / promo landing — kept out of the repo (hosted separately)
docs/

# Legacy source assets (old-board fonts/icons + demo media) — unused by the build
assets/
